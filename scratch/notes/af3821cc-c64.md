# Task af3821cc-c64 — UpdateRule.lean CI Fix

## Claim advanced
Fixed CI build failure in `CausalGraphTheory/UpdateRule.lean`.

## Root cause
1. `spawnPredicate_symm` used non-existent `beq_comm` and invalid `ring_nf`.
2. The theorem was mathematically false without `h_energy` hypothesis (non-commutative octonions).
3. First fix attempt used `Bool.and_congr_right_iff` which was unknown.

## Files modified
- `CausalGraphTheory/UpdateRule.lean`:
  - Rewrote `spawnPredicate_symm` with `h_energy` hypothesis.
  - Used `Bool.beq_iff_eq` and `eq_comm` for symmetry proofs.
  - Removed unused `Fin.ext_iff` simp arg to silence linter.

## Build status
`lake build` passed. `pytest calc/` passed. No `sorry`.

## Next step
Advance GAUGE-001. Callers of `spawnPredicate_symm` need `h_energy`.