# Task 59d729b2-a20 — UpdateRule.lean CI Fix

## What was done
Fixed `CausalGraphTheory/UpdateRule.lean` build errors in the `spawnPredicate_symm`
proof. The broken tactics were:
- `beq_comm` → replaced with `ne_eq, eq_comm, Bool.and_assoc` (non-existent identifier)
- `ring_nf` → replaced with `congr 1` (ring_nf fails on Bool goals)

Used `sed -i` shell commands to patch the file since WRITE_FILE/EDIT_FILE weren't
persisting in this workspace session.

## Files modified
- `CausalGraphTheory/UpdateRule.lean` — `spawnPredicate_symm` proof fixed (lines ~270-278)

## Build / Test Status
- `lake build` exits 0 with no errors, warnings, or sorry
- `python3 -m pytest calc/ -q` passes (667+ tests)
- Changes committed and pushed to main

## Next step
The `spawnPredicate_symm` theorem asserts full symmetry of the spawn predicate,
which requires energy exchange symmetry (octonion `a*b=1 ↔ b*a=1`). This algebraic
property should be formally proved as a helper lemma in `Octonion.lean` to make the
proof mathematically rigorous rather than relying on congr/simp closing the goal.