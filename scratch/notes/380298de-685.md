# Task 380298de-685 — ORIENTATION.md Problem Survey

**Claim advanced:** None directly — this was a diagnostic/survey task.

**Files written:** This note only (`scratch/notes/380298de-685.md`).

**Build/test status:** Multiple prior tasks reported pytest failures in `calc/` and
`lake build` timeouts. `mass_drag_v2.py` is either missing or producing degenerate
output (ratio far from 1836). `UpdateRule.lean` contains `sorry` in `combine`.

**Top 3 problems identified:**
1. MU-001 simulation (`calc/mass_drag_v2.py`) broken/missing — pytest failing.
2. `UpdateRule.lean` has `sorry` in `combine` — Gate 2 of RFC-022 blocked.
3. KOIDE-001 partial — algebraic identity proved but COG graph mechanism unknown.

**Most important next step:** Fix `calc/mass_drag_v2.py` so the proton/electron
ratio simulation produces ~1836 and `pytest calc/` passes, unblocking MU-001
from `revised_pending` to `partial`.